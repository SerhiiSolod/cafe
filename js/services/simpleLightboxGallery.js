const lightboxgallery = new SimpleLightbox(".gallery-cards__wrapper a", {
  captionDelay: 200,
  showCounter: false,
  maxZoom: 2,
  scrollZoomFactor: 0.1,
});

const lightboxStaff = new SimpleLightbox(".working-staff a", {
  captionDelay: 200,
  showCounter: false,
  maxZoom: 2,
  scrollZoomFactor: 0.1,
});
